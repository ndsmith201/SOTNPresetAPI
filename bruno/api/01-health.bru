meta {
  name: Health
  type: http
  seq: 2
}

get {
  url: {{base_url}}/healthz
  body: none
  auth: none
}

tests {
  test("HTTP 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  Public process health check. This does not probe DynamoDB.
}
